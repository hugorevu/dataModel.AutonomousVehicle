allOf:
  - $ref: "Observation.yaml"
  - type: object
    title: GNSSObservation
    description: Observación específica de GNSS para ubicación
    properties:
      observedProperty:
        $ref: "GNSSObservableProperty.yaml"
      hasResult:
        type: object
        description: Valores medidos por el sensor GNSS
        properties:
          latitude:
            type: number
            format: float
            description: Latitud medida por el sensor
          longitude:
            type: number
            format: float
            description: Longitud medida por el sensor
          altitude:
            type: number
            format: float
            description: Altitud medida por el sensor
