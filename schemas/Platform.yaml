type: object
title: Platform
description: Entidad genérica que puede contener sensores
properties:
  id:
    type: string
    description: Identificador único de la plataforma
  type:
    type: string
    description: Tipo de plataforma
  sensors:
    type: array
    description: Lista de sensores montados en la plataforma
    items:
      $ref: "Sensor.yaml"
